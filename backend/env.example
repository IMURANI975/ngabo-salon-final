PORT=5000
MONGODB_URI=mongodb://localhost:27017/ngabo-saloon
NODE_ENV=development
JWT_SECRET=replace-with-secure-secret

# Create an admin in MongoDB using a script/Compass:
# Admin fields: username, email, password, name
